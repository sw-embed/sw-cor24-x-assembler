Step 1 shipped: spi_sdcard_read.s + tests/programs/sdcard-test.img fixture + test_spi_sdcard_read_demo integration test using EmulatorCore-equivalent direct attach (cpu.io.spi.device = Some(Arc::new(Mutex::new(SdCardDevice::from_file(...))))). End-to-end CLI verification works against the now-shipped --spi-device flag (sw-cor24-emulator/main 59a7a97). 124688 instructions to print first 16 bytes of sector 0. Added to non_halting for the no-device case (CMD0 wait hangs). Refactored CMD55/ACMD41 into subroutines to fit brf range.
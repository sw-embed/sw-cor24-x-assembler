In src/assembler.rs, the '.comm' arm of the directive match contains a nested 'if parts.len() >= 3' that clippy (collapsible_match) flags. Collapse the nested if into a match-arm guard so the body is reached only when the length check passes. Preserve behaviour: label defined at current address, address advanced by size, no bytes emitted. Verify ./scripts/build.sh is fully green (build + clippy -D warnings + test).
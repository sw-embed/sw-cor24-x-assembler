Published i2c_add1_ping.s standalone per mike's urgent brief dcxas-publish-i2c-add1-ping (dwxas was blocked on this for the web add1 dropdown entry). Originally scoped this saga as two steps (add1 + tmp101) but mike's brief redirected to ship add1-only via pr/publish-i2c-add1-ping; tmp101 is no longer required because dwxas's TMP101 panel uses the pre-assembled tmp101.lgo path (not a .s source). New file: src/examples/assembler/i2c_add1_ping.s (~190 lines including inlined i2c bit-bang primitives — no .include in this assembler). Verified end-to-end via cor24-emu --i2c-device add1@0x50 producing '43' output. Registered in tests/integration_tests.rs::examples() at alphabetical position (between Fibonacci and Literals). All workspace tests + clippy clean.
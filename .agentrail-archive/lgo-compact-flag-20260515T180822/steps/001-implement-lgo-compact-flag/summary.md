Added --lgo-full / --lgo-compact flag pair to cor24-asm. Default = Full (today's bit-identical behavior). Compact omits L records whose entire data payload is 0x00; loads correctly in cor24-emu and on FPGA cold boot, not safe on warm reload. Mutually exclusive flags; both passed → exit 2 with stderr mentioning both names. Threading: pub LgoMode { Full, Compact } in src/lgo.rs; lgo::write() now takes mode; CLI tracks Cli.lgo_mode as Option<LgoMode> to distinguish default-vs-explicit for the mutex check. Tests: 8 unit tests for Compact mode behavior in src/lgo.rs (skip pure-zero, keep partial-zero, keep G with all-zero data, byte-identity for non-zero lines, round-trip through loader, default unchanged), 4 CLI integration tests (default = explicit Full, strips zero lines, smaller for zero-heavy input, mutex exit-2), 1 emulator round-trip integration test (Full and Compact execute identically). README CLI block updated. Smoke verified: 256-byte zero region collapses 584 → 104 bytes.
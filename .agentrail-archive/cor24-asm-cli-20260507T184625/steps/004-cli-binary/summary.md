Replaced cli/src/main.rs stub with full implementation. Hand-rolled arg parsing, three writers wired up (.lgo default, --bin, --listing), exit codes 0/1/2, IsTerminal refusal for binary stdout. Smoke-tested all paths end-to-end: file in / file out (all three artifacts), stdin→stdout .lgo pipe, broken-asm → exit 1 with stderr, missing file → exit 2, --help / -V output. Listing output matches emulator's --assemble byte-for-byte.
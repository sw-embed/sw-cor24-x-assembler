Updated README: added ## CLI section above ## Library Usage covering all four invocation forms, exit codes, and TTY-refusal rule. Renamed existing ## Usage to ## Library Usage. Updated ## Build with note about cor24-asm binary path. scripts/build.sh now uses --workspace for build/clippy/test (clippy also gets --tests so the integration tests are linted). ./scripts/build.sh runs to completion green: 20 lib tests + 14 module tests (lgo + listing) + 11 CLI integration tests + 0 doctests, all passing.
Update CLAUDE.md to include the devgroup branching/PR policy, AgentRail session protocol (with branch → begin → work → commit → complete → mark-pr), .agentrail/ directory rules, key rules, useful commands, and cross-repo context — matching the style used in sibling sw-embed CLAUDE.mds (e.g. sw-cor24-plsw, sw-cor24-pcode). Keep existing Rust build/architecture/dependencies sections.
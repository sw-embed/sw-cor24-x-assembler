Added .zero N directive to cor24-asm. New arm in handle_directive (src/assembler.rs:288) parses N via parse_number, emits N zero bytes, advances address. Spelling matches GNU as and partner brief dcpls-emit-zero-fill default. Works in both .text and .data (flat memory model — no segment restriction). 8 unit tests cover: N=5, N=1024, N=0 no-op, byte-identity vs .byte 0,0,...,0, in .data, in .text, label resolution after .zero, hex literal count. CLI integration test (cli/tests/cli.rs:zero_directive_round_trips_through_cli) round-trips a mixed .zero/non-zero fixture through .lgo, .bin, and --listing. Full workspace check clean (build/test/clippy/version).